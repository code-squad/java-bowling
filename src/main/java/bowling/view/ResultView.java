package bowling.view;

import bowling.domain.BowlingGame;

public class ResultView {

    public static void print(BowlingGame bowlingGame) {
        /*
        System.out.println(bowlingGame.toString());

        while(bowlingGame.done()) {
            int inputShot = InputView.inputShot(bowlingGame.getCurrentFrame());
            //bowlingGame.play(inputShot);
            System.out.println(bowlingGame.toString());
        }
        */
    }
}
